#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    ll n;
    cin >> n;
    ll x, y;
    for(ll i=0; i<n; i++)
        cin >> x;
    for(ll i=0; i<n; i++)
        cin >> y;
    cout << "Karen" << "\n";

    return 0;
}

